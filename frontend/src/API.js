const BASE = '/api';

const API = {
    USER: BASE + '/user',
    SIGNUP: BASE + '/signup',
    LOGIN: BASE + '/login',
    LOGOUT: BASE + '/logout',
    SESSION_EXPIRED: BASE + '/session-expired',
    POST: BASE + '/post',
    TAG: BASE + '/tag',
    FILES: BASE + '/files'
};

export default API;